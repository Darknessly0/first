// CommonJS, every file in node is module (by default) 
// Modules - Enacapsulated Code (only share minimum)
const names = require('./4-names');
const syHi = require('./5-utils');
// console.log(names);
const data = require('./6-altvflavor');
require('./7-mind');

// console.log(data);

// syHi('amien');
// syHi(names.samir);
// syHi(names.ali);